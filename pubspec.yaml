name: gitrinth
description: A tool for managing modpacks on modrinth using git.
version: 0.1.0
# repository: https://github.com/my_org/my_repo

executables:
  gitrinth:

environment:
  sdk: ^3.11.0

workspace:
  - tool/asset_strings_builder

dependencies:
  args: ^2.7.0
  path: ^1.9.0

dev_dependencies:
  asset_strings_builder: any
  build_runner: ^2.4.0
  lints: ^6.0.0
  test: ^1.25.6
