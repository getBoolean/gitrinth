slug: {{slug}}
name: {{name}}
version: {{version}}
description: {{description}}

loader:
{{loader-block}}
mc-version: {{mc-version}}

tooling:
  gitrinth: ">={{gitrinth-version}} <{{gitrinth-next-major}}"

mods:
  globalpacks: stable

resource_packs:

data_packs:

shaders:

plugins:
