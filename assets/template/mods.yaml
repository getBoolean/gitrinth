slug: {{slug}}
name: {{name}}
version: {{version}}
description: {{description}}

loader:
{{loader-block}}
mc_version: {{mc_version}}

gitrinth:
  version: ">={{gitrinth-version}} <{{gitrinth-next-major}}"

mods:
  globalpacks: stable

resource_packs:

data_packs:

shaders:

plugins:
