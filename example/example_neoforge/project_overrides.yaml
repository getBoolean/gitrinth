# yaml-language-server: $schema=../../assets/schema/project-overrides.schema.yaml
project_overrides:
  # Pin a version but keep the default Modrinth source.
  # jei:
  #   version: 19.27.0.340

  # Redirect to a local build.
  # create:
  #   path: ./mods/create-dev.jar
